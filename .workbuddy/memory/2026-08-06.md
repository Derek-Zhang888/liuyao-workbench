# 2026-08-06 工作日志

## 六爻工作台 v0.10 改进建议2（#1-#12）

### 范围
用户 `D:\知识库1\六爻工作台V0.10改进建议2.md` 12 条 bug，按用户硬性要求「先改 bug 后加功能最后改布局」。

### 已落地（commit dd5b6b9，领先 origin/main 3 提交）
- **#1 钱币卦**：qigua.js `HEADS_TO_VALUE` 按「背为阳」约定反向（0正=老阳9、1正=少阴8、2正=少阳7、3正=老阴6）；QiguaSelector 钱币卦渲染 `flex-col-reverse` 实现「上爻在上、初爻在下」传统盘面布局；PanView 变卦列加爻画渲染（`bian.lines` 初→上 6 位 1/2 串）；PanView 干支行加 `solarTime: 'HH:mm'` 字段显示「时刻 HH:mm」
- **#2 爻名卦**：YAO_TYPES label 1→少阳 2→少阴（统一与钱币卦用词）；加 `重置` 键
- **#3 切辅助工具页不重置**：新增 `INPUT_SESSION_KEY='liuyao-qigua-input-state'`，QiguaSelector 14 个 input state（method/dateMode/dt/lunar/coinFaces/yaoSpins/guaBen/guaBian/num1-3/numMethod/digits/compMode/compSeq）全部 useEffect 持久化 + mount 恢复；PaipanPage handleReset 同步清除
- **#4 卦名卦别名**：qigua.js 新增 `GUA_NAME_ALIASES = { '水火未济': '火水未济' }`，findGuaByName 与 searchGuaByName 均支持；浏览器实测输入「水火未济」自动识别为「火水未济」
- **#5 数字卦**：QiguaSelector 三个 number input `min="0"`，doQiGua 校验 `n < 0` 才报错（0 合法）；引擎 qiguaFromNumber 早已支持 n>=0（0÷8=8、0÷6=6）。加 `重置` 键
- **#6 报数卦**：qiguaFromBaoshu 重写校验：`normNum(s, max, fallback)` 允许 0-8 / 0-6，0→fallback（卦数位 8、动爻位 6）。UI 加 `重置` 键
- **#7 时间卦六神**：经独立万年历锚点（1900-01-01 甲戌）+ paipan.test.js 6 个六神断言 + 浏览器实测（壬子日 初爻玄武、上爻白虎）：引擎 100% 正确（日干起初爻顺排）。用户原文「六兽排错了」可能是参考 App 用法差异，未找到参考 App 规则截图，建议下轮澄清
- **#8 电脑卦**：compTypeLabel 静阳/静阴 → 少阳/少阴（统一术语）
- **#9 删除分秒卦**：qiguaFromMinuteSecond + QIGUA_METHODS.fenmiao + paipan dispatch.fenmiao + QiguaSelector UI + importMd parseInput + exportMd PARAMS_SERIALIZER + FM_GUIDE 提及 + 相关测试，全链路清理。QIGUA_METHODS 现 7 项
- **#10 标签删除联动**：tagsRepo.deleteTag 改为单事务（tags + guashi 两个 objectStore）：删标签 + 遍历 guashi.getAll() 对含该标签名的记录 put 更新；返回 `{ removedFromGuashi }`。新建 `src/components/ConfirmDialog.jsx` 自定义弹窗（Esc/Enter 键盘 + 遮罩点击取消 + 「不再提醒」勾选写 localStorage `liuyao-noremind-<key>`）；GuashiLibPage.handleDeleteTag + TagEditor.requestDelete 都改用新弹窗；端到端实测：保存带「占病」标签的卦例 → 删除标签 → 打开详情确认 tags 已移除
- **#11 批量导出 zip**：新建 `src/utils/zip.js` 手写 zip（Stored 模式 + CRC32 + UTF-8 general purpose bit 11 + DOS 时间），不引外部依赖；exportBatch.downloadGuashiBatch 改为 `createZip(uniquifyNames(...))`，文件名 `卦例导出-YYYY-MM-DD-HH-mm.zip`，处理同名追加 `(1)(2)...`。浏览器实测确认下载文件为 zip
- **#12 占断页清空按钮**：PaipanPage 删 `handleRestoreDefault` 调用，按钮文案改 `恢复默认`→`清空`，`handleClearDuan` 只清 `title + duan`，保留 tags/pan/saved/记忆（sessionStorage 的 duan 也对应更新）

### 关键决策
- [decision] **「六兽（六神）错」未在代码层定位到 bug**：引擎 + UI + md 导出 + 浏览器实测全部正确（壬日 初爻玄武 验证通过）。建议下轮向用户索要具体复现场景或参考 App 六神截图对照。已记录但本轮未代码改动
- [decision] **不引入 jszip**，手写极简 zip（Stored 模式 + CRC32）以避免 npm 依赖波动；与 PKWARE APPNOTE 6.3.x 兼容
- [decision] **ConfirmDialog 复用**：GuashiLibPage + TagEditor 共用同一 localStorage key `liuyao-noremind-tag-delete`——用户在一处勾选不再提醒，另一处同样静默删除
- [decision] **QiguaSelector 持久化策略**：只持久化输入区 14 个 state，不持久化 pan/duan/tags（这些由 PaipanPage 自己的 SESSION_KEY 处理）；handleReset 清掉两处 key 实现彻底重置

### 测试
- `npx vitest run` → 9 files / **158 passed**（基线 154，+zip 6 -fenmiao 2 = +4）
- 新增 `src/utils/zip.test.js` 6 项（CRC32 标准向量 0xCBF43926 + 空文件 + 单/多文件结构 + UTF-8 标志 + CRC 一致性）
- qigua.test.js：报数卦 0 测试、4 种钱币结果按新映射断言、混合序列风泽中孚
- paipan.test.js：删 fenmiao 分派用例
- exportMd.test.js / importMd.test.js：删 fenmiao 序列化与解析断言
- guashiRepo.test.js：原 `deleteTag 不影响卦例` 改为 `deleteTag 联动移除 + 不影响未含该标签的卦例` 两条新测试

### 提交
- `dd5b6b9` fix: 六爻工作台 v0.10 改进建议2（#1-#12 + 联调），测试 158 绿
- 20 文件改动（13 修改 + 4 新增 + 3 已存在但加入）
- 领先 origin/main 3 提交（7cad84c、369ec6c、dd5b6b9），**待 push**

### 待办（下一会话）
- [ ] git push 3 个新提交
- [ ] 用户确认「六兽（六神）错」具体复现：如某日某卦参考 App 显示 vs 工作台显示，定位差异
- [ ] 如确为参考 App 与标准日干起法不同，可考虑 PanView 变卦列加六神列（用户 #1 「变卦也要显示出阴阳爻」已部分覆盖）

---

## 六爻工作台 v0.10 改进建议3（7 条，commit 71c2da5）

### 范围
用户 D:\知识库1\六爻工作台V0.10改进建议3.md 7 条 bug，全部完成并浏览器实测通过。

### 已落地
- **#1 变卦游魂/归魂标注**：PanView 卦名行变卦后补 `bian.youhun/guihun`（与 ben 一致）；实测 天水讼(离宫·游魂) → 山风蛊(巽宫·归魂)
- **#2 地支五行**：PanView LiqinText 改为 `{zhi}{wuxing}`（如 戌土、寅木），本卦/变卦/伏神统一；实测每爻「孙 戌土」「父 寅木」
- **#3 农历中文**：paipan.js 新增 cnNum/cnMonth/cnDay（正月/初一/初十/廿X/三十 边界齐全），lunarDate 如「农历2026年六月廿四」（末尾无「日」字，与用户例「九月十六」一致）
- **#4 卦名模糊搜索变体**：qigua.js 新增 `aliasKeywords(k)`——对「水火/火水」互换与别名多关键词展开；输入「水火」候选即含 火水未济 + 水火既济；实测 ✓
- **#5 切页保留输入（重点修复）**：QiguaSelector 输入持久化从「恢复 useEffect + firstRenderRef 跳过写入」重构为 **useState 惰性初始化（readInput helper）**——根因是 **React StrictMode 双执行 effect**：写入 useEffect 的 setup 跑两次（第一次 skip 置 false、第二次用空 state 覆盖 sessionStorage），导致恢复永远读不到旧值。实测切卦辞页→回排盘 Tab 与输入均保留 ✓
- **#6 报数卦恢复「数字不能为 0」**：qiguaFromBaoshu 恢复原校验（前两位卦数 1-8、后动爻 1-6，含 0 抛 RangeError）；UI 说明同步；注意：**这与建议2 #6 的「能输入 0」相反，以建议3 为准**
- **#7 标签命中数排序**：GuashiLibPage filtered 有标签筛选时按命中标签数降序（`[...list].sort((a,b)=>hitCount(b)-hitCount(a))`）；**验证要点：单标签时所有命中卦例命中数相同排序不变，需多标签才能看到效果**；实测「占病+工作」时 22354/9002/9001（2命中）排最前 ✓

### 关键决策
- [decision] 建议3 #6 报数卦「数字不能为0」**覆盖**建议2 #6「能输入0」——用户需求反复，以最新文件为准；数字卦（建议2 #5）不受影响仍可输入 0
- [solution] **React StrictMode 双执行 effect 陷阱**：mount 时无依赖 useEffect 的 setup 会执行两次（dev 下），用「跳过首次渲染」flag 不可靠；持久化恢复用 useState 惰性初始化最稳
- [pattern] Vite HMR 在浏览器调试时不可靠（改了代码要 location.reload 全刷新再测，否则可能验证的是旧 bundle）

### 测试
- `npx vitest run` → 9 files / **161 passed**（上轮 158 +3：农历边界 +2、搜索变体 +3、报数卦 0 拒绝 -2）
- paipan.test.js：农历正则改中文 + 正月/初一 边界
- qigua.test.js：报数卦 0 抛错恢复 + searchGuaByName 变体 3 条

### 提交
- `71c2da5` fix: 六爻工作台 v0.10 改进建议3（7 条），测试 161 绿
- 8 文件（7 修改 + .workbuddy/memory 新增）
- 领先 origin/main **4 个提交**（369ec6c、7cad84c、dd5b6b9、71c2da5），**待 push**

### 待办（下一会话）
- [ ] 用户确认「六兽（六神）错」（若下轮仍无复现则保持跳过）

---

## 六爻工作台 v0.10 改进建 4（9 条，commit ff370ed）

### 范围
用户 D:\知识库1\六爻工作台V0.10改进建 4.md 9 条 bug，全部完成并浏览器实测通过。
用户额外指示：取消 git push 待办；六兽错标记为已解决忽略。

### 已落地
- **#1 卦名搜索单字"水"命中未济**：qigua.js `aliasKeywords` 加「别名反向变体」——输入 k 时遍历别名表，把键/标准名含 k 的加入候选。**根因**：搜"水"前 8 候选按表序，火水未济排第 9 被 limit=8 截断；实测修复后首条 ✓
- **#2 吉凶必选 + 对错去留空**：DuanInput `DuiCuoRow` 选项从 3 个 ['对','错','留空'] 改 2 个 ['对','错']；吉凶按钮去掉清空逻辑（点同一按钮不取消）；PaipanPage `handleSave` 加 `!duan.jixiong` 校验
- **#3 卦例库新增标签 + 反馈→筛选**：GuashiLibPage 加 `AddTagInline` 组件（与 TagEditor 共用 tags 表）；筛选栏「反馈」文案改「筛选」
- **#4 统计页放第二 + 去标签筛选**：App.jsx `MAIN_NAV` 顺序 排盘→统计→卦例库；StatsPage 移除标签筛选 section（保留三总览卡 + 三正确率卡）
- **#5 统计页点击跳转 + URL query 筛选**：GuashiLibPage 筛选状态全部走 `useSearchParams` 作单一来源（status/jixiongOk/yingqiOk/fangweiOk 四个 key）；StatsPage 总览卡（总/已反馈/未反馈）+ 正确率卡（对/错）按钮点击 `navigate('/lib?...')` 跳转
- **#6 备注↔反馈互换 + 取消已反馈清空对错**：DuanInput JSX 调整 + `useEffect` 监听 status 从 已反馈→未反馈 自动清空 `jixiongOk/yingqiOk/fangweiOk`（用 onChangeRef 避免 effect 依赖循环）；md 正文 section 顺序 反馈/备注 互换
- **#7 取消统计页错题本**：StatsPage 移除「错题本」section 与相关 state（wrongOn/wrongList/wrongChip/expanded），文件代码 -147 行
- **#8 已反馈展开六项对错筛选 + 卡片标志**：GuashiLibPage 在 statusFilter==='已反馈' 时展开 6 个对错按钮（吉凶对/错、应期对/错、方位对/错）；GuashiCard 加 `CompassIcon`（指南针 SVG）+ 应期🕰/方位标志位，已反馈带 ✓/✕、未反馈仅图标
- **#9 md 盘面补太岁 + 列序调整**：exportMd renderPan head 加 `太岁：${pan.yearGZ}` + `月建：${pan.monthGZ}`（取代原"月建：未"仅地支）；表列序改为 五行|爻画|世应（删原旺衰列）；md 正文 section 反馈/备注 互换

### 关键决策
- [decision] 统计页的「错题本」是用户提出 #7 主动要求删除（之前的设计），不是「修 bug」——按用户最新意愿走
- [decision] GuashiLibPage 筛选改 URL 单一来源：避免 React state ↔ URL 双向同步的循环，依赖关系更清晰，浏览器刷新/分享链接可保留筛选
- [decision] DuanInput 取消已反馈清空用 `useRef(onChange)` 引用最新回调避免 effect 依赖 onChange 触发循环
- [pattern] Vite HMR 偶有编译错误（如函数签名被破坏），实测前 `location.reload()` + 控制台 500 错误是检测手段

### 测试
- `npx vitest run` → 9 files / **161 passed**（与上轮持平）
- exportMd.test.js 盘面表头断言更新为新列序 + 太岁干支正则
- stats.test.js 不依赖已删组件字段（测试的是 stats.js 纯函数），保持全绿

### 提交
- `ff370ed` fix: 六爻工作台 v0.10 改进建4（9 条），测试 161 绿
- 9 文件（7 修改 + 删减 stats.test.js 不变 + 净 -31 行：StatsPage -147 行，新增 AddTagInline + CompassIcon + URL 筛选 + 修正 GuashiCard）
- 领先 origin/main **6 个提交**（369ec6c→ff370ed），按用户意愿**不再 push 待办**

### 待办（下一会话）
- 无（git push 已按用户指示取消）

---

## 六爻工作台 v0.10 改进建 5（6 条，commit 5992de8）

### 范围
用户 D:\知识库1\六爻工作台V0.10改进建 5.md 6 条 bug，全部完成并浏览器实测通过。

### 已落地
- **#1 占断页方位文本框**：DuanInput 在应期下加「方位」textarea；PaipanPage `EMPTY_DUAN` 与会话恢复、GuashiLibPage `duanOf` 补 `fangwei` 字段；卦例库未反馈卡片方位标志随 `(fed ? fwOk : fw)` 生效
- **#2 卦例库筛选/编辑状态切页保留**（重点）：
  - 筛选 query：单 effect「先恢复后保存」——mount 时 URL 无 query 且会话有旧值 → setSearchParams 恢复并 return（本帧不保存），等下一帧再保存；**免疫 StrictMode 双执行 effect 的覆盖坑**（同 QiguaSelector #5 的教训）
  - 编辑卦例 id：存 `liuyao-lib-editing-id`；mount 恢复；返回列表走 `backToList()` 清理会话（避免下次挂载误恢复）；统计页跳转带 query 时以 query 为准覆盖会话
  - 实测：未反馈筛选 → 统计 → 回卦例库 URL 保留 + 高亮；编辑页 → 统计 → 回卦例库保持编辑视图
- **#3 时钟标志改文字**：GuashiCard 底部 🕰 emoji →「应期」文字、指南针 SVG →「方位」文字（删 CompassIcon）；已反馈显示 应期✓/应期✕、方位✓/方位✕；未反馈仅文字
- **#4 未反馈展开子筛选**：statusFilter==='未反馈' 时展开 吉/凶/应期/方位 四按钮（URL key：jixiong=吉|凶、yingqi=1、fangwei=1）；filtered 增加对应过滤
- **#5 筛选栏位置互换 + 清除全清**：筛选行（状态+对错/子项+搜索）移到标签行上面；`clearFilters` 改 `setSearchParams({})` 清全部 URL 参数（含已反馈六项对错、未反馈子项）
- **#6 PanView 简化**：移除爻位神煞显示（华盖/驿马等，y.shensha 渲染删除）；表头「本卦 · 爻画 · 世应」→「本卦」（爻位旁世/应角标保留）；引擎仍输出 shensha（md 未用，不影响测试）

### 关键决策
- [decision] StrictMode 下「恢复+保存」必须**同 effect 先恢复后保存**（恢复时 return 不保存，下一帧再保存）——彻底规避双执行 setup 覆盖 sessionStorage
- [decision] 编辑会话清理放 `backToList()`：显式用户行为才删，组件卸载/重挂载不误删

### 测试
- `npx vitest run` → 9 files / **161 passed**（无新增用例，逻辑改动均浏览器实测）
- 实测覆盖：方位文本框出现、筛选栏顺序（筛选 101px < 标签 145px）、未反馈四按钮展开、切页筛选/编辑保留、卡片文字标志（吉应期/凶应期/吉方位）

### 提交
- `5992de8`，领先 origin/main **7 提交**（369ec6c→5992de8），按用户意愿不 push

### 待办（下一会话）
- 无
---

## 追加修复（commit 953fe14）：卦例库「全部」按钮无法选中

用户实测报 bug。根因：建议5 #2 的「先恢复后保存」effect 在每次 searchParams 变化时都尝试恢复会话旧值——点「全部」清空 URL 后，effect 又用 sessionStorage 旧筛选拉回状态。
修复：恢复只允许首次挂载执行（filterInitializedRef 标记），之后只保存不恢复。
验证：「全部」可选中不再被拉回；#2 切页恢复仍生效。测试 161 绿。
