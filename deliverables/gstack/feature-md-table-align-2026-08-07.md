# 导出 MD 盘面表格重构交付报告（爻位反转 + 变卦列 + 源码等宽对齐）

**日期**：2026-08-07
**场景**：用户验收反馈后的格式修正（走团队 SOP：工程师实现 → QA 两轮独立验证）
**参与成员**：主理人（编排）+ 工程师（实现）+ QA（独立验收）
**团队**：software-md-align

---

## 📌 TL;DR
- 整体结论：🟢 **Go**（QA Round 2 最终判定 NoOne；全量 **17 files / 290 tests** 全绿；build 通过）
- 用户三个反馈点全部落地：① 爻位反转（初爻在最底）② 补变卦列 ③ 源码级等宽对齐
- 过程中 QA 抓到 1 个 P1 缺陷（旧快照导出崩溃）→ 工程师已修复 → Round 2 回归通过

---

## 1. 需求落地（用户原话逐条）

| 用户反馈 | 落地结果 |
|---------|---------|
| 「六个爻位颠倒的，要反过来，初爻在最底下」 | 表格行序反转：**第 1 行 = 上爻，最后 1 行 = 初爻**（FM_GUIDE 说明同步改） |
| 「没有变卦，要补上」 | 每爻补 **变卦 4 列**（变卦六亲/地支/五行/爻画），方案 B：六神列后加「爻位」列，总 11 列；无变卦（无动爻）留空 |
| 「要对齐，五行列对齐五行、爻画也是，新加变卦也要对齐」 | **源码级等宽对齐**：中文按 2 半角宽计，每单元格 padEnd 固定列宽，分隔线等宽——md 源文件打开即整齐（非依赖渲染器） |
| 「不明白的问我，有建议的问我」 | 已确认：方案 B（含爻位列）；**变卦不加天干列**（纳干开启时仅本卦 12 列） |

### 列结构（最终）
```
无纳干（11 列）：| 六神 | 爻位 | 六亲 | 地支 | 五行 | 爻画 | 世应 | 变卦六亲 | 变卦地支 | 变卦五行 | 变卦爻画 |
纳干开启（12 列）：| 六神 | 爻位 | 六亲 | 天干 | 地支 | 五行 | 爻画 | 世应 | 变卦六亲 | 变卦地支 | 变卦五行 | 变卦爻画 |
```

## 2. 关键技术点
- 变卦爻数据：`parseLiqin(bian.liuqin[5-i])`（liuqin 上→初序，5-i 取同爻位）+ `bian.lines[i]`（初→上序按 i 直取，纯数字不带动爻 ●）
- 等宽工具：`dispWidth`（中文/全角=2、ASCII=1）、`padCell`、`renderRow`、`renderSep`
- 伏神行：六神列「伏神」、爻位列留空、六亲/地支/五行填伏神值（全称）、其余留空
- 只改 `exportMd.js` + `exportMd.test.js`；importMd.js/paipan.js/PanView.jsx 未动，导入端天然兼容

## 3. QA 两轮验证
- **Round 1**（独立探针 6115 断言）：行序/变卦列/48 组合源码对齐/纳干 11·12 列/伏神/导入可逆/FM_GUIDE 全过；发现 **P1：旧快照（bian 有 liuqin 无 lines，8/6 之前形态）导出崩溃**（`bian.lines[i]` TypeError）→ 路由 Engineer
- **修复**：`bian?.lines?.[i] ?? ''` 可选链 + 补旧快照回归用例
- **Round 2**：P1 复测不再崩、旧快照变卦爻画列留空、6115 断言复跑全过、全量 290 tests 全绿、build 通过 → **NoOne 放行**

## 4. 变更文件
- `src/md/exportMd.js`（renderPan 表格段重构 + parseLiqin/dispWidth/padCell/renderRow/renderSep + FM_GUIDE 两行文案）
- `src/md/exportMd.test.js`（27 → 34 测试）

## 5. 遗留与提醒
- 未 commit（工作区大量未提交改动照旧，建议整体收口 git）
- 项目根曾有历史遗留未跟踪探针（_check_tmp.mjs 等，非本次产物），本次 QA 探针已清理
- 环境备忘：Windows 单 worker 跑测试（多 worker V8 OOM）；build 前需 PowerShell 清 dist（safe-delete shim 拦截）；后台 vitest 勿接 `| tail` 管道
